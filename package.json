{
  "name": "ft-next-handlebars",
  "version": "0.0.0",
  "main": "express.js",
  "scripts": {
    "test": "make test",
    "release": "npm-prepublish --verbose && npm publish && git checkout package.json"
  },
  "dependencies": {
    "dateformat": "^1.0.11",
    "es6-promise": "^2.0.1",
    "express-handlebars": "^1.2.1",
    "handlebars": "^3.0.0"
  },
  "devDependencies": {
    "chai": "^2.1.2",
    "mocha": "^2.0.1",
    "npm-prepublish": "^1.0.2",
    "sinon": "^1.14.1",
    "supertest": "^0.15.0"
  }
}
